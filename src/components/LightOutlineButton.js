const LightOutlineButton = ({ children }) => {
    return (
        <button className="btn btn-outline-light rounded-0 sec-font">
            {children}
        </button>
    );
};

export default LightOutlineButton;
